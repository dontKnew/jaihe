'use client'
import { useRouter } from "next/navigation"; // in client component

export default function Home() {
  const router = useRouter();
  // return router.push("/rewardpoint");
  return null;
}
